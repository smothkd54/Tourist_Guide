# Core ML — pinned to TF 2.19+ which ships with Keras 3 and supports
# `import keras` natively.  The old `from tensorflow import keras` shim
# was removed in recent TF; using `import keras` is now the standard.
tensorflow>=2.19.0,<2.23
numpy>=1.26.4

# Image processing
pillow==10.4.0

# Training visualisation (only needed for train.py / evaluate.py,
# not for serving — see requirements-serve.txt if you want a
# slimmer production image without matplotlib)
matplotlib==3.9.0

# API server
flask==3.0.3
flask-cors==4.0.1

# Image collection scripts
requests==2.32.3
beautifulsoup4==4.12.3

# Crop tool
opencv-python==4.10.0.84

# Deduplication
imagehash==4.3.1

# Production WSGI server (also in requirements-serve.txt)
gunicorn==22.0.0

# Testing (not needed in production)
pytest==8.2.2